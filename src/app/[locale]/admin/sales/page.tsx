import { SectionPlaceholder } from "@/components/admin/SectionPlaceholder";

export default function Page() {
  return (
    <SectionPlaceholder permission="sales.view" titleKey="admin.sales" />
  );
}
