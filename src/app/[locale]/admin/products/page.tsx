import { SectionPlaceholder } from "@/components/admin/SectionPlaceholder";

export default function Page() {
  return (
    <SectionPlaceholder permission="products.view" titleKey="admin.products" />
  );
}
